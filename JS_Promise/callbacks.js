const posts = [
  { tile: 'Post One', body: 'This is the first post'},
  { tile: 'Post Two', body: 'This is the second post'},
];

const timeout = 1000;

function getPosts() {
  setTimeout(() => {
    let output = '';
    posts.forEach((post) => {
      
    });
  }, timeout);
}